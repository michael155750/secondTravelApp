package com.example.secondtravelapp;

import androidx.lifecycle.MutableLiveData;

import com.example.secondtravelapp.Repository.ITravelRepository;
import com.example.secondtravelapp.Repository.TravelRepository;
import com.example.secondtravelapp.Models.Travel;
import com.example.secondtravelapp.Models.UserLocation;

import org.junit.Test;

import java.util.Date;
import java.util.List;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
   /* ITravelRepository repository = TravelRepository.getInstance();
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
    Travel travel;

    {
        try {
            travel = new Travel("Moshe", "0588887777", "moshe@gmail.com",
                        new UserLocation(12, 243), 60,
                        new Date(12, 30, 2020), true, true,
                        new UserLocation(20,2000), new UserLocation(120, 2030));
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @Test
    public void acceptCompany_isCorrect() {
        String name = "Dan";
        travel.setCompany(name);
        repository.acceptCompany(name, travel);
    }


    @Test
    public void acceptTravel_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }


    @Test
    public void sentSuggestion_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }


    @Test
    public void getAllTravels_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }

    @Test
    public void getAllClientTravels_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }

    @Test
    public void getCompanyTravels_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }

    @Test
    public void getAllHistoryTravels_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }
    @Test
    public void getIsSuccess_isCorrect() {
        repository.addTravel(travel);
        assertEquals(4, 2 + 2);
    }
*/
}

